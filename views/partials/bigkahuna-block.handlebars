<li>
	{{hamburger_name}}

	<button class="devoured-state ml-4" data-id="{{hamburger_id}}" data-newburger="{{hamburger_devoured}}">
		{{#if hamburger_devoured}}Eat Burger{{else}}Queue Burger{{/if}}
	</button>
</li>